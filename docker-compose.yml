
services:
  db:
    container_name: "dbsheet"
    image: postgres:16
    env_file:
      - .env.development
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
volumes:
  db_data: